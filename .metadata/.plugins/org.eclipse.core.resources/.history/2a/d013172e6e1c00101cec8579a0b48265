package abstraction;

public class User implements Login{

	@Override
	public boolean login(String username,String password) {
		if(username.equals("Bahvana")&& (password == "bhavana@123"));
		
	}

}
